# 安全策略

Relay Terminal 会启动本地 Shell、保存模型 API Key，并可能把经过脱敏且有长度限制的近期终端上下文发送给用户配置的模型端点。该上下文按标签页隔离、只保存在进程内存中，并在标签页关闭、Shell 退出、AI 关闭或模型切换时释放。安全问题应通过私密渠道报告，不要在公开 Issue 中披露可利用细节、密钥或真实终端输出。

## 支持版本

当前仅维护最新的 `main` 分支和最新发布版本。`0.1.x` 仍处于首版开发阶段，安全修复可能要求升级 Electron 或其他核心依赖。

## 报告漏洞

请使用 GitHub 的私密安全报告入口：

https://github.com/jiachengg6666-collab/relay-terminal/security/advisories/new

报告应尽量包含：

- 受影响的版本、提交和操作系统
- 可复现的最小步骤
- 预期行为与实际行为
- 影响范围和可能的利用条件
- 已去除 API Key、密码、个人路径和终端敏感输出的日志

维护者确认问题前，请不要公开漏洞细节。普通功能缺陷可以使用公开 Issue。

## 安全边界

- 模型建议不会自动执行；用户必须检查并确认最终命令。
- 本地风险分级和脱敏是纵深防护，不是绝对安全保证。
- 用户负责确认自定义模型端点的可信度、数据处理条款和访问权限。
- 未签名、未公证的开发构建不应作为正式公开发行包分发。
